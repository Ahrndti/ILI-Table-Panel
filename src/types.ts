export interface PanelOptions {
  fontSize: string;
  showHeader: boolean;
  wordWrap: boolean;
}

export const defaultOptions: PanelOptions = {
  fontSize: '12px',
  showHeader: true,
  wordWrap: false,
};
