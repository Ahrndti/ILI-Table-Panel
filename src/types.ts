export interface PanelOptions {
  fontSize: number;
  showHeader: boolean;
  wordWrap: boolean;
}

export const defaultOptions: PanelOptions = {
  fontSize: 12,
  showHeader: true,
  wordWrap: false,
};
